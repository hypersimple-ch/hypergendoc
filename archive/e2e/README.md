# Archived end-to-end tests

These browser, real-stack, renderer, security, and load suites are intentionally inactive. They are excluded from formatting, linting, type checking, and test discovery.

The active quality gate is `pnpm check`, which runs unit, component, service, route, repository, contract, and focused security regression tests.

Restore this directory only if full end-to-end coverage becomes worth maintaining again.
